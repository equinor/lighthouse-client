import styled from 'styled-components';

/**
 * Styled component for wrapping Popover content with predefined tag styles.
 */
export const PopoverContainer = styled.div`
    white-space: nowrap;

    hr {
        border: 1px solid #dcdcdc;
    }

    h5 {
        margin-top: 0;
        margin-bottom: 0;
    }

    p {
        margin: 0;
        font-size: 12px;
    }
`;
