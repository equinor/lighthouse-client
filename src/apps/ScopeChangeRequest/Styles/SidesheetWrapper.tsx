import styled from 'styled-components';

export const Wrapper = styled.div`
    position: relative;
    height: 100%;
    z-index: 100;
    background: #fff;
    width: 1200px;
    grid-area: sidebar;
    margin: 0em 0.5em;
`;
