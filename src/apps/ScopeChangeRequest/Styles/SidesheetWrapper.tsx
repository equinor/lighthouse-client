import styled from 'styled-components';

export const Wrapper = styled.div`
    position: relative;
    height: 100%;
    z-index: 100;
    background: #fff;
    max-width: 1200px;
    grid-area: sidebar;
    margin: 0em 1em;
    overflow: scroll;
`;
