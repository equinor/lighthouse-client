import styled from 'styled-components';

export const Wrapper = styled.div`
    position: relative;
    height: calc(100vh - 64px);
    z-index: 100;
    background: #fff;
    width: 650px;
    min-width: 650px;
    grid-area: sidebar;
`;
