import styled from 'styled-components';

export const Wrapper = styled.div`
    position: relative;
    height: 100%;
    z-index: 100;
    background: #fff;
    max-width: 1200px;
    grid-area: sidebar;
    margin-left: 1.5em;
    overflow: scroll;
`;
