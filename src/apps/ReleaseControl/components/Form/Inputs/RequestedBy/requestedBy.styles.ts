import styled from 'styled-components';

//Custom made component for the small text above the selection since all the other headers/components comes from EDS-core-react
export const StyledRequestedByHeader = styled.div`
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    margin: 0px 8px;
    font-size: 0.75rem;
    line-height: 1.333em;
    color: rgba(111, 111, 111, 1);
`;
