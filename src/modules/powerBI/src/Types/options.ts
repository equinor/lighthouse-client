export interface PBIOptions {
    showFilter?: boolean;
    enablePageNavigation?: boolean;
    defaultPage?: string;
    activePage?: string;
    pageLoad?: boolean;
    isFilterActive?: boolean;
    aspectRatio?: number;
}
